[pytest]
pythonpath = .
testpaths = shop
python_files = tests_*.py test_*.py
# Produce coverage.xml so `testcheck` can report coverage gaps in one pass.
addopts = -v --cov=shop --cov-report=xml --cov-report=term-missing
